# VotePrivacy
This is a privacy focused voting system that utilizes identity commitment, not physical identity. Private data never leaves the user's device only the generated proof is submitted to the chain. Midnight utilizes zk-SNARKs for the proof, a class of proofs designed for compact size and efficient verification. https://github.com/topics/midnightntwrk
